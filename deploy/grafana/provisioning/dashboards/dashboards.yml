apiVersion: 1
providers:
  - name: capacity
    orgId: 1
    folder: Capacity
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
